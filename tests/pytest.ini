[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show test names, not just dots
addopts = -v --tb=short

# Markers
markers =
    slow: marks tests as slow (LLM calls)
